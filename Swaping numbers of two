#include<stdio.h>
int main()
{
    int a=30,b=50;
    printf("before SWAP a=%d and b=%d",a,b);
    a=a+b;//a=80(30+50)
    b=a-b;//b=30(80-50)
    a=a-b;//a=50(80-30)
    printf("/n After SWAP a = %d b= %d ",a,b );
    return 0;
    
    
}
